#include<bits/stdc++.h>
using namespace std;
int main(){
    #define int long long int 
    int t;
    cin >> t;
    while(t--){
        int n;
        cin >> n;
        vector<int> v(n);
        map<int, int> mp1, mp2;
        for (int i = 0; i < n; i++)
        {
            cin >> v[i];
        }
        for (int i = 2; i < 2 * n;i++){
            
        }
    }
    return 0;
}